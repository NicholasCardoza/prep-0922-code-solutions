var languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];

console.log('languages array', languages);

var firstElement = languages[0];

console.log('the first element of the array is html', firstElement);

var thirdElement = languages[2];

console.log('the 3rd element of the array is javascript', thirdElement);

var length = languages.length;

console.log('the lenght of the array is', length);

var lastIndex = length - 1;

var lastElement = languages[lastIndex];

console.log('the last element in the array is', lastElement);
